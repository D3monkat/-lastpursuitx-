-- If set to nil, the queue process will be faster but more resource expensive
-- If set to a number between 0 and 5 (inclusive), the queue process will be slower but less resource expensive
QUEUE_TIME_TO_WAIT = 5